import React, { PropsWithChildren } from 'react'

const ObjectLegendActionsWrapper = ({ children }: PropsWithChildren) => {
  return <div className='flex items-center'>{children}</div>
}

export default ObjectLegendActionsWrapper
